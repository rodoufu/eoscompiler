const container = require('./container');

const Wallet = {
    create: (name) => {
    },
    open: (name) => {
    },
    list: () => {
    },
    keys: () => {
    },
    unlock: (name) => {
    },
    createKey: (name) => {
    },
    import: (name) => {
    }
};

module.exports = Wallet;
